# Repair iteration

Read the captured failure evidence and reproduce the smallest failing case. Classify the failure, locate its root cause, implement the minimal robust fix, rerun the focused and related tests, then run the fast quality gate. Do not weaken tests, typing, coverage, or security checks. Record evidence and atomically update loop state. After three identical signatures mark the task blocked and continue no blind retries.
