# GeoForge Studio bounded iteration

Read `AGENTS.md`, `LOOP_GOAL.md`, `LOOP_STATE.json`, and `TASKS.md`. Select the highest-priority open task that fits one bounded work package. Implement it with focused tests, repair root causes, run the fast quality gate, update relevant documentation and task status, append a concise evidence record to `LOOP_LOG.md`, and atomically update `LOOP_STATE.json`. Stop after this one package. Never request approval, network access, or filesystem access outside this repository.
