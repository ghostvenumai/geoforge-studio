# Security review

Inspect upload/download containment, YAML parsing, CSV injection, CORS, headers, logs, dependencies, secrets, denial-of-service controls, and container privilege. Reproduce findings safely with synthetic data and record exact evidence.
