# Interview Preparation

Be ready to explain why a modular monolith was chosen: it keeps the portfolio runnable while preserving service boundaries. Discuss Polars for columnar execution, Pydantic discriminated step configs, checksum-based reproducibility, SQLite as a local metadata plane, and the migration path to PostgreSQL plus durable workers.

For deduplication, describe postal-code blocking, maximum block size, weighted exact/normalized/fuzzy scores, review thresholds, and canonical strategies. For quality, emphasize that quarantine and unexplained row loss are shown beside score improvements. For security, explain safe YAML, fixed operators, path containment, formula-injection protection, local-only egress, and non-PII logs.

Use measured facts: 82 Python tests, 92.54% coverage, 3 passing Playwright scenarios, zero known Python dependency vulnerabilities, no Bandit high findings, and 2,300 pipeline rows/s on the final 100k run. State the Docker DNS blocker honestly.
